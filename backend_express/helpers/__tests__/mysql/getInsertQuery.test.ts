import { getInsertQuery } from '../../mysql/getInsertQuery';

import faker from 'faker';

describe('getInsertQuery helper function should render valid string for mysql query execution', () => {
    test('', () => {

    })
});
